name: my-env

channels:
    - conda-forge
    - bioconda
    - defaults

dependencies:
    - minimap2
    - samtools
    - bedtools
    - r-base
    - r-ggplot2
    - r-dplyr
    - r-tidyverse